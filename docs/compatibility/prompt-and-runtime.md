# Prompt and Runtime Compatibility Contracts

Baseline: v1.10.0, commit `165ec46`. This registry freezes observable behavior; known validation and trust-boundary gaps remain repair requirements rather than accepted compatibility.

## Prompt and Tool Output

| ID / status | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `PR-001` Preserve + Gap — request augmentation | Released DeepSeek request objects with a string `prompt`; arbitrary sibling fields and `ref_file_ids` are accepted. | For valid input, returns a JSON body that preserves sibling fields, replaces `prompt`, and may set `model_type`; also returns task prompt, used memory IDs, and message count. | Invalid JSON, missing prompt, or another falsy prompt currently returns `null`. Valid non-object JSON or a truthy non-string prompt is not decoded and can throw; this is a gap, not accepted input. Future schemas must preserve unknown siblings unless explicitly rejected by a versioned adapter. | `null` silently means “send the original body”; a thrown validation/construction error rejects the intercepted request. Unknown Skill prefixes currently fall back to an ordinary prompt. | Only the explicit `null` cases recover the original request body; thrown errors have no automatic recovery. Construction itself has no persistent side effect. | `core/interceptor/request-augmentation.ts`; `tests/request-augmentation.test.ts` |
| `PR-002` Preserve — augmentation byte order | Existing preset, system/memory, project, web guidance, language, user prompt, and tool descriptor inputs. | Preset plus separator, joined system sections, visible-user block, then tool-format reminder in exactly that order. | New sections may not be inserted or reordered without byte-golden review and an explicit compatibility decision. | Construction errors propagate through the augmentation RPC; there is no prompt-level partial-success record. | Only explicitly recognized extension-unavailable/context-invalidated failures recover by returning `null` and using the original body; other construction failures reject the intercepted request. | `core/prompt/augmentation.ts`; `tests/prompt-output-contract.test.ts`; `tests/fixtures/prompt-output/` |
| `PR-003` Preserve — visible user markers | Prompts containing the released markers. | `<!-- deepseek-pp-visible-user-prompt:start -->\n${prompt}\n<!-- deepseek-pp-visible-user-prompt:end -->`; history sanitization extracts the enclosed text. | Missing or mismatched markers return no extraction; future markers require backward parsing of this exact pair. | Extraction returns `null`; sanitizer may use its caller-provided fallback or remove the tool reminder. | The marked block is the reversible path from augmented prompt to visible user text. | `core/prompt/visibility.ts` |
| `PR-004` Preserve + Gap — inline-agent continuation | Released traces using `<original_task>`, `<tool_results>`, `<tool_results_so_far>`, and `<previous_assistant_text>`. | Deterministic continuation/nudge text; task 8,000 chars, previous text 4,000, result detail 4,000, output 8,000; truncation suffix is `...[truncated]`. The loop increments its nudge counter before the prompt builder adds one again, so the first real correction currently displays attempt 2. | Unknown tags remain text. Size changes or new tags require byte fixtures and historical trace replay. Correcting the visible nudge count requires an explicit compatibility decision rather than silently updating the golden. | Loop construction failures produce `AGENT_LOOP_ERROR`; truncation is visible in generated text. | Abort completes the loop with `AGENT_LOOP_COMPLETE` and empty `finalText`; original trace remains available. | `core/inline-agent/prompt.ts`; `core/inline-agent/loop.ts`; `tests/inline-agent-*.test.ts`; `tests/fixtures/prompt-output/inline/` |
| `PR-005` Preserve — inline-agent completion | Released `<task_complete>` blocks with JSON or text bodies. | JSON `{summary:string,artifacts:string[]}` becomes final summary; invalid JSON uses trimmed raw body and an empty artifact list. | Missing block returns no signal; future fields may be ignored but existing summary/artifact meaning cannot change silently. | Invalid JSON is observable only through the raw summary fallback; no dedicated error code exists. | History cleanup uses the same conversion. Stored traces retain task/prompts and normalized step/final text, not the original `<task_complete>` bytes. | `core/inline-agent/prompt.ts`; `tests/inline-agent-prompt.test.ts`; `tests/history-cleanup.test.ts` |
| `TL-001` Preserve — tool model records | Released serializable descriptor, call, result, error, execution-context, execution-history, and restore records. | Provider kinds `local|mcp`; modes `auto|manual|disabled`; risks `low|medium|high`; one reusable codec authority validates every released record family and nested JSON value before cross-runtime consumption. | Legal records and serializable unknown sibling fields remain accepted. Missing required fields, unsupported provider identities, non-JSON payloads, and malformed nested restore arrays are rejected. | Malformed bridge records are discarded before handlers; malformed direct runtime calls return non-retryable `tool_call_payload_invalid` before history or provider I/O. | No universal side-effect rollback. `retryable` is advisory and callers must not retry ambiguous side effects automatically. | `core/messaging/tool-record-codec.ts`; `core/tool/runtime.ts`; `tests/tool-record-contract.test.ts` |
| `TL-002` Preserve + Gap — XML invocation | Released direct XML and legacy DSML tool-call text. | Preferred closed form is `<invocation_name>{JSON object}</invocation_name>`; legacy DSML remains readable; generic `<tool_call>` and `<invoke name>` wrappers are deliberately not executed. | Unknown tags are ignored. Non-object JSON yields `tool_call_payload_invalid`; malformed JSON yields `tool_call_json_invalid`. An unclosed tag must never execute, but its buffered body/visible text is currently discarded at flush and remains a current-only gap. | Parse failure is retained in `ToolCall.parseError`, not thrown or treated as execution success. | Parser is side-effect free; closed-call stripped visible text remains. Recovery of incomplete buffered text is not implemented. | `core/tool/invocation.ts`; `core/interceptor/tool-parser.ts`; `core/tool/xml-tags.ts`; parser tests |
| `TL-003` Preserve + Gap — streaming tool events | Released streaming XML and externalized large-payload records. | Emits started/completed calls and `{id,invocationName,chunk}` chunks; eligible payloads over 64,000 chars are externalized; retained raw text is capped at 2,048 chars. | Malformed payloads use the same parse codes; unknown or unclosed tags never become completed calls. External payload entries expire after 30 minutes and `take` consumes once; an invocation-name mismatch currently also deletes the entry, while name/chunk binding is not validated earlier. | Missing, expired, consumed, or mismatched external payload yields retryable `tool_call_external_payload_missing`; no provider executes. | Recovery requires obtaining the payload again, not guessing or partial execution; T1.2 freezes the current one-shot mismatch deletion as a gap. | `core/interceptor/streaming-tool-call-parser.ts`; `core/tool/externalized-payload.ts`; `tests/prompt-output-contract.test.ts`; XML stream tests |
| `TL-004` Preserve + Gap — runtime tool execution | Content sends released `EXECUTE_TOOL_CALL` with a legal `ToolCall`. | Background accepts it only from the trusted DeepSeek top-frame content surface, validates the record codec, and returns the released `ToolResult`; boundary rejection is also ToolResult-shaped and non-retryable. | Unknown tools still fail `tool_unsupported`; missing/invalid runtime results still fail `runtime_tool_result_missing|invalid`. T2.2 owns descriptor/provider/mode/risk/call/session authorization without changing legal calls or results. | Sender, surface, and record failures are visible before provider execution. Stale/replay/cross-session authorization remains a T2.2 gap. | Only explicitly safe cases may retry. History quota failure warns but does not erase an otherwise completed tool result. | `core/messaging/runtime-boundary.ts`; `core/messaging/tool-record-codec.ts`; `core/tool/runtime.ts`; Issue #317 |

## Runtime, MAIN/Content Bridge, and Sandbox

| ID / status | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `RT-001` Preserve + Gap — runtime command router | Released `{type,payload}` names handled by background. | Before the existing 119-case router, the listener requires a plain typed envelope, constructs browser-owned sender/tab/frame/document context, and applies a 28-command DeepSeek-content allowlist; extension contexts preserve the released router. The exact 119/89/32/2 names remain frozen in the [Runtime Command Inventory](./runtime-command-inventory.md). | Thirty-two live cases are absent from the union, two union variants have no case, and most command payloads remain casts. T3.1 establishes one exhaustive map without a second router. | Invalid envelope or sender is explicit; unauthorized content commands fail before the router. Unknown authorized extension commands still return ambiguous `null` until T3.1. | Recovery is domain-specific; no global transaction or rollback exists. | `core/messaging/runtime-boundary.ts`; `entrypoints/background.ts`; `tests/runtime-boundary.test.ts` |
| `RT-002` Preserve + Gap — runtime responses | Released callers accept raw arrays/config records, `{ok:true}`, `{ok:false,error}`, `ToolResult`, or `null` depending on command. | Legal handler responses are unchanged. Boundary failures return `{ok:false,error:runtime_message_*}`; execution-command boundary failures return non-retryable failed `ToolResult`. | New commands require a serializable request/response/error contract before registration. The unused `core/messaging.onMessage` helper is not a live response contract. | Envelope, context, and authorization failures are explicit; handler-domain errors remain command-specific and unknown authorized extension commands remain ambiguous. | No shared retry marker outside `ToolError.retryable`; domain operations own recovery. | `core/messaging/runtime-boundary.ts`; `entrypoints/background.ts` |
| `RT-003` Preserve — broadcasts and tab RPC | Released update/notification names plus three tab RPCs. | Tab RPC remains `REFRESH_DEEPSEEK_AUTH`, `GET_CURRENT_DEEPSEEK_CONVERSATION`, `INSERT_PROMPT_TEXT`. Content receivers now require the extension ID/origin and the generated Chrome/Edge service-worker or Firefox background-page path before state, auth-header, prompt insertion, or conversation I/O. Notification names remain unchanged. | Unknown notifications from the trusted background are ignored by listeners. Other extension/page senders are rejected before dispatch. | Many send failures remain best effort; rejected receiver context is explicit on the main listener and ignored by the sidebar-only notification listener. | State consumers can reread storage; progress and stream notifications have no replay. | `entrypoints/content.ts`; `entrypoints/content/adapters/project-sidebar-organizer.ts`; `tests/runtime-boundary.test.ts` |
| `BR-001` Preserve — bridge handshake | Released window message request/init and transferred `MessagePort`. | Names and retry timing are unchanged. Receivers additionally require a plain object, exact origin, `event.source === window`, top-level frame, no prior connection, and exactly one transferred port for init. Each Port handler is bound to a receiver-owned document session object; `pagehide`/`messageerror` invalidates it, stale handlers have zero dispatch, and BFCache restore opens a new epoch. | MAIN-world page code shares page identity, so the handshake is routing/freshness rather than authentication; every subsequent port payload remains untrusted and decoded. Shape-valid calls from the current bridge remain routing claims: descriptor/call/session authorization, replay, and cross-session rejection belong to T2.2. | Invalid handshake and exhaustion remain silent; bridge disconnect rejects pending augmentation instead of leaving it unresolved. | Content queues outbound events in memory until ready; BFCache recovery restarts the bounded handshake with a new session and has no durable replay. | `core/messaging/schema.ts`; both content entrypoints; `tests/bridge-schema.test.ts` |
| `BR-002` Preserve — request augmentation RPC | Released `AUGMENT_REQUEST_BODY`, extension timeout, and result messages correlated by string `id`. | Default wait is 8s; result contains `ok`, modified body/task prompt or `null`, and optional error. | Unknown/expired IDs are ignored. Extension-unavailable/context-invalidated currently degrades to successful `result:null`; other errors reject. | Timeout and non-context errors are visible; compatibility degradation is only visible as no augmentation. | `null` uses the original request body; pending entries are deleted on settle/timeout. | two runtime entrypoints; `core/messaging/schema.ts` |
| `BR-003` Preserve — bridge event envelope | Released bridge types `SYNC_HOOK_STATE`, augmentation triplet, `TOOL_CALL_STARTED`, `TOOL_CALL_CHUNK`, `TOOL_CALL`, `RESTORE_TOOL_CALLS`, `RESPONSE_COMPLETE`, `RESPONSE_TOKEN_SPEED`, `MEMORIES_USED`, `HEADERS_CAPTURED`, `DPP_BRIDGE_READY`. | One direction map and per-type decoder validate every nested payload, descriptor, call/restore record, ID, header, response, and token-speed field before dispatch; legal JSON round trips and `HEADERS_CAPTURED:null` are preserved. | Unknown types, wrong direction, non-plain objects, non-JSON tool payloads, and malformed nested fields are discarded. | Invalid messages are silent and produce no handler/storage/tool side effect. | Restore records can rebuild tool UI; other events have no acknowledgement or replay. | `core/messaging/schema.ts`; `core/messaging/tool-record-codec.ts`; `tests/bridge-schema.test.ts` |
| `SB-001` Preserve — `sandbox_run` tool request | Released sandbox tool calls with `{language,code,input?,timeoutMs}`. | Tool normalizer accepts `javascript|typescript|python|html`; empty/invalid/over-30KB code returns `sandbox_invalid_request`; timeout clamps to 1-15 seconds; final surface is `ToolResult`. | Unknown languages do not expand execution capability. Future languages require a new allowlisted codec and manifest review. | Runtime unavailable, invalid input, worker errors, and timeouts have explicit codes. | Timeout terminates worker/frame. In-memory effects already produced by approved code are not rolled back. | `core/sandbox/types.ts`; `core/sandbox/tool.ts`; interactive tool tests |
| `SB-005` Preserve — `RUN_ARTIFACT_CODE` | Released runtime action with a legal `SandboxRunRequest`. | Background applies the same language, non-empty code, 30,000 UTF-8-byte limit, and timeout normalizer as `sandbox_run` before creating the offscreen document; legal requests still return `ToolResult`. | Unknown request sibling fields are explicitly stripped. New languages or larger inputs require a contract and manifest review. | Invalid input returns non-retryable `sandbox_invalid_request`; offscreen, worker, and timeout failures retain their released codes. | Timeout terminates worker/frame; no rollback of already produced in-memory effects. | `core/sandbox/tool.ts`; `entrypoints/background.ts`; `tests/sandbox-contract.test.ts` |
| `SB-002` Preserve — background/offscreen port | Released port `sandbox-offscreen`, `OFFSCREEN_SANDBOX_RUN`, and `OFFSCREEN_SANDBOX_RESULT` correlated by `requestId`. | Offscreen accepts the port only from this extension's generated Chrome/Edge service-worker or Firefox background-page path; strict request/result envelopes require a non-empty ID and nested payload/result before dispatch. Background still adds 2 seconds to execution timeout. | Unknown IDs are ignored. A safely correlated malformed result settles immediately as `sandbox_invalid_result` rather than waiting for timeout. | Invalid request/result, disconnect, and timeout have explicit codes. | Settle removes the pending request and disconnects; no replay. | `core/sandbox/contracts.ts`; background/offscreen entrypoints; `tests/sandbox-contract.test.ts` |
| `SB-003` Preserve — offscreen/frame bridge | Released `DPP_SANDBOX_RUN` and `DPP_SANDBOX_RESULT` with `requestId`; frame payload adds `pyodideBaseUrl`. | Opaque sandbox frames still require `postMessage('*')`, bounded by exact WindowProxy, expected extension/opaque event origin, strict envelope/result codecs, and receiver-owned request correlation. Pyodide URL is restricted to extension `/pyodide/`. | Unknown or stale IDs are ignored; malformed correlated requests/results return explicit invalid codes. | Invalid request/result and timeout are visible in result. | Pending run is deleted on settle; frame is reusable; no execution replay. | `core/sandbox/contracts.ts`; offscreen and sandbox-runner entrypoints; `tests/sandbox-contract.test.ts` |
| `SB-004` Preserve — worker and HTML result | Released worker input `{code,input,outputLimit,pyodideBaseUrl?}` and output `{ok,stdout,stderr,result?,truncated,error?}`; HTML uses request-correlated log/error/done messages. | Worker behavior is unchanged; frame/HTML events now require exact source/origin, non-empty correlation, and type-specific nested fields before settling. | Unknown, stale, duplicate, wrong-source, wrong-origin, or malformed events are ignored or become a safely correlated invalid result. | Errors are returned, never treated as successful output. | Worker terminates and HTML frame/listener is removed on settle/timeout; Pyodide cache lives only inside worker lifecycle. | `core/sandbox/worker-runner.ts`; `core/sandbox/python-worker.ts`; sandbox runner; contract tests |

## T1.2/T1.3 Freeze Requirements

- Generate actual byte-level goldens for prompt combinations and inline-agent continuations; source-snippet hashes alone are insufficient.
- Establish an exhaustive runtime command/request/response/error fixture that documents the present 119/89 completeness gap; T3.1 eliminates the gap.
- Establish per-type legal and malformed bridge fixtures that expose current shallow validation, plus correlation, timeout, disconnect, and replay behavior; T2.1 owns receiver decoding and enforcement.
- Keep tool/sandbox failure and retry semantics explicit; never invent rollback for side effects that cannot be proven reversible.

## Current Evidence Limit

T1.2 replaces the source-snippet hashes with four raw UTF-8 golden files generated only from production functions. The executable cases cover composed memory/Skill/preset/project context, MCP and Shell descriptors, Chinese chat and English thinking prompts, direct and qualified XML names, inline continuation/nudge output, current truncation boundaries, the hidden continuation placeholder, and the current one-shot externalized-payload mismatch behavior. `npm run prompt:freeze` is read-only and reports a line diff; golden updates require the explicit `npm run prompt:freeze:update` command.

T1.3 adds executable request, response, error, and malformed fixtures for runtime commands, all 13 port bridge types, tool wire records, sandbox requests/results/errors, 17 notifications, three tab RPCs, and sandbox multi-hop envelopes. The runtime topology is derived through the TypeScript AST and paired with a 121-entry data-only contract registry rather than duplicated as a production router.

T2.1 turns the frozen bridge, tool-record, and sandbox fixtures into receiving-boundary codecs; adds browser-owned runtime sender/tab/frame/document context plus surface authorization; restricts content tab RPC and offscreen Port senders to the background entrypoint; and unifies the sandbox request size policy before privileged I/O. Legal fixtures remain byte-compatible. The remaining `32/2` command split and 69 direct payload casts belong to T3.1; descriptor/provider/risk/stale/replay/cross-session tool authorization belongs to T2.2.
