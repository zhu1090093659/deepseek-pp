# Persistence and Sync Compatibility Contracts

Baseline: v1.10.0, commit `165ec46`. The production code owns 35 fixed `chrome.storage.local` keys, two fixed `chrome.storage.session` keys, and three IndexedDB databases. R3.9 removed the unused arbitrary-key platform adapter; new keys still require an explicit registered contract and a production consumer.

For unversioned single-key records, the minimum future rule is: keep the key, preserve readable historical values, reject corrupt or explicitly future-versioned data before writing, and never overwrite original data with defaults merely because decoding failed. A table row calling out current defaulting/filtering describes present behavior, not permission to repeat silent loss.

## IndexedDB Identity

| ID / status | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `DB-001` Preserve + Bounded Gap — Memory DB | `DeepSeekPP` v1-v3, table `memories`, auto-increment `id`; v1 indexes plus v2 `syncId`, v3 `scope,projectId`. | The sole Memory codec accepts released v1→v3 upgrades and exact v3 rows, including additive fields and an empty description. Every ordinary read or mutation validates the complete current table; JSON batches validate before one locked Dexie transaction. | The production repository verifies native DB version 3 explicitly. A newer version or corrupt row rejects before an ordinary write, sync replacement, or import and leaves raw rows untouched. New versions require deterministic, idempotent upgrades without changing DB/table/key identity. | Open, codec, transaction, and broadcast-facing repository failures reject visibly; Content and Side Panel keep the last valid state instead of projecting false empty success. | Transactional imports/replacements are all-or-nothing, and the recovery journal restores released raw rows and explicit IDs exactly. IndexedDB cannot rewind its hidden `++id` generator, so a rolled-back higher target ID may leave a harmless future-ID gap; no row, existing ID, retry identity, or reference is lost, and no second allocator is introduced. | `core/memory/codec.ts`; `core/memory/schema.ts`; `core/memory/store.ts`; Memory repository, compatibility, sync, and UI boundary tests. |
| `DB-002` Preserve — Artifact DB | `DeepSeekPPArtifacts` v1, table `artifacts`, key `id`, index `createdAt`; `deepseek_pp_artifacts` is released migration input. | IndexedDB is the sole runtime truth. The exact-preserving Artifact codec validates the complete table and every new row; a legal legacy array is decoded before mutation, merged transactionally without pruning, verified, then its key is removed. Ordinary saves retain the committed row plus the newest 49 prior rows even after clock rollback. | The production repository verifies native DB version 1 explicitly. Newer DBs, corrupt rows, malformed/duplicate legacy arrays, and true same-ID value conflicts reject without changing IndexedDB or deleting the legacy key. Chrome-storage omission and IndexedDB preservation of an optional `undefined` object property are the released canonical equivalent, while nested/additive value differences remain conflicts. | IndexedDB/storage absence, migration, codec, cleanup, and commit-indeterminate failures reject. Failed migrations reset the session promise and retry; there is no runtime storage fallback. | Migration is restart-safe and idempotent. A lost legacy-key remove response is read back; only verified absence counts as cleanup success. All legal rows, nested values, and additive fields survive, including legacy arrays larger than the retention limit; invalid repository input writes nothing. | `core/artifact/schema.ts`; `core/artifact/store.ts`; Artifact persistence, dual-write convergence, migration interruption/restart, conflict, future-version, clock-rollback retention, and unavailable-IDB tests. |
| `DB-003` Preserve — Local-state recovery DB | `DeepSeekPPSyncRecovery` v1, table `journal`, key `&id`, singleton `current`; introduced after the v1.10.0 baseline. | The unchanged schema-v1 record contains an operation ID, timestamp, SHA-256-protected raw Memory rows, and opaque present/value preimages for the six sync-owned local keys. The same coordinator now journals both sync apply and Project/Memory cascade deletion; journal absence is the only local commit state. | Unknown kind/version, malformed shape, or checksum mismatch is preserved and fails closed before local mutation. Future versions require an explicit upgrade; they must never be cleared as if recovery succeeded. | Prepare, apply, rollback, verification, and clear failures reject. A lost write/delete response is read back; an unverifiable delete is reported as commit-indeterminate and remains behind the recovery barrier. | Any surviving journal is uncommitted and restores the full raw preimage in reverse store order on startup. Released Memory rows/IDs/unknown fields, raw key values, and key absence are restored exactly; the hidden IndexedDB generator limitation remains the bounded `DB-001` gap. | `core/sync/apply-journal.ts`; `core/sync/local-apply.ts`; `core/sync/local-apply-runtime.ts`; sync and generic mutation fault/restart tests. |

## Fixed `chrome.storage.local` Keys — Versioned and High-Risk

| ID / key | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `LS-001` `deepseek_pp_artifacts` | Released bare `ArtifactRecord[]`; migration input only. | A complete legal array is merged into the fixed Artifact IndexedDB identity without pruning, verified, then removed. It is never read or written as a runtime fallback. | Non-array, corrupt/duplicate rows, or conflicts reject before mutation and preserve the original key. No envelope version is invented for this retired input. | Storage/API, decode, merge, and cleanup failures reject; a failed attempt is retryable in the same session. | The key remains until IndexedDB convergence and cleanup are both verified. Lost remove responses are read back; successful removal makes `DB-002` the sole truth. | `core/artifact/store.ts`; Artifact fake-IndexedDB migration/retry/idempotency tests. |
| `LS-002` `deepseek_pp_automations` | Released `{version:1,automations,runs}` plus decimal-string/empty message IDs, missing per-automation DeepSeek session state, runner requests without `deadlineAt`, and orphan runs that the pre-serialization delete/create race could legally leave behind. | One exact-preserving codec emits v1, projects only those released aliases, and derives the historical 180-second deadline. Reads and mutations share the existing Automation-local queue; claims persist one `running` lease atomically, terminal writes fence that row, and history keeps 100 runs per automation. Obsolete free-form run append/create writers are removed. | Explicit future root or automation-record versions, missing versions, corrupt/duplicate rows, and invalid nested contracts reject before clocks, UUIDs, or writes and preserve the raw key. Historical orphan runs remain readable. `AbortSignal` and callbacks remain in-process only. | Codec/storage failures reject and do not poison the queue. Automation UI reports load failures without replacing its last confirmed list. Interrupted leases remain explicit non-retryable ambiguous failures. | Whole-key rewrite has no rollback, but queued read-modify-write prevents same-realm stale replacement. Restart preserves a fresh `running` lease; an expired lease closes and advances without replay, and late terminal writes are rejected. | `core/automation/storage-codec.ts`; `core/automation/store.ts`; raw compatibility fixtures; storage-contract, concurrency, reconciliation, and lease-restart tests; `AU-001` |
| `LS-003` `deepseek_pp_mcp_servers` | v1 server/config/cache records, including legacy Shell allowlist. | A strict codec reads/writes legal v1 records; missing storage yields empty v1 without an eager write. | Future state/server versions, unknown transports, and corrupt/duplicate/orphan records reject before UUID generation or mutation; the raw key is not downgraded or overwritten. | Typed codec and storage errors reject visibly without exposing secret values. | Decode failure leaves the whole key untouched; cache remains rebuildable after legal config is restored, while configuration/secrets are preserved in the original raw record. | `core/mcp/storage-codec.ts`; `core/mcp/store.ts`; `core/shell/policy.ts`; MCP persistence/policy and Shell policy tests |
| `LS-004` `deepseek_pp_project_context` | Released v1, v2, and versionless v2 project/conversation state. | One domain codec emits v2. Released v1 projects, sources, files, active IDs, and additive fields survive exactly while `conversations=[]` and `pendingProjectId=null` are added; reads do not eagerly rewrite, and the first successful mutation persists the migrated state. | Explicit future versions, corrupt records, duplicate IDs, broken references, or corrupt Memory rows reject before clocks, UUIDs, journal preparation, or business writes. Local, sync, and Side Panel boundaries reuse the same codec. | Storage/codec/journal failures reject; Project UI and sync report them visibly. | Decode failure leaves raw state untouched. Project deletion stages validated Project and Memory state, then reuses the existing full-preimage recovery journal and startup barrier; every pre-commit failure restores both stores exactly, while a cleared journal keeps both deletions. | `core/project/codec.ts`; `core/project/store.ts`; local-apply runtime/coordinator; compatibility, repository, sync, and cascade fault tests. |
| `LS-005` `deepseek_pp_saved_items` | v1 object, versionless `{items}`, and legacy bare item array. | One exact-preserving codec writes `{schemaVersion:1,items}`; legal strings, duplicate tags, order, and additive fields are not normalized during decode. Reads do not eagerly rewrite legacy input. | Explicit future versions and corrupt items reject before clocks, IDs, or writes. Local, sync, and Side Panel boundaries reuse the same codec. | Storage/codec failures reject and Saved Items UI shows them instead of rendering false empty state. | Decode failure leaves the raw key untouched. Sync replacement first validates current raw state and the existing local-apply journal restores the opaque preimage after later failures. | `core/saved-items/codec.ts`; `core/saved-items/store.ts`; compatibility, repository, sync-schema, and Side Panel tests. |
| `LS-006` `deepseek_pp_skills` | Released bare custom/remote `Skill[]`; missing `enabled` means true, empty descriptions and historically possible duplicate names remain readable. | The sole Skill codec deeply validates released remote metadata and preserves additive fields. Bundled Skills remain outside this key. Local and sync collection reads reuse the same codec without adding an envelope. Name-keyed enable changes update every historical duplicate; ambiguous duplicate-custom edits fail without collapsing records. | Present null/scalar/object envelopes, unsupported per-record versions, corrupt fields, unknown user sources, and malformed mutation fields reject without filtering, fallback, or overwrite. | Decode/storage/runtime failures reject; Content and Skill UI retain the last confirmed snapshot, and the inline Skill-draft action requires explicit `{ok:true}`. | Same-realm mutations use the existing local-state lock. Imported-source upsert, Skill deletion, and source deletion stage Skills and Sources together through the existing full-preimage recovery journal; deleting a duplicate name reconciles every linked source, and failure or restart restores both raw keys exactly. | `core/skill/codec.ts`; `core/skill/registry.ts`; Background local-mutation runner; raw compatibility, sync-parity, UI-boundary, duplicate-record, and cross-key recovery tests |
| `LS-007` `deepseek_pp_skill_sources` | Released bare GitHub/local source arrays; local providers remain local-only and an absent historical sync file remains supported. | The sole source codec validates provider-specific fields, deep arrays, and additive fields. Local and sync paths share it; one shared policy excludes local sources from upload and preserves them during download merge. Ambiguous duplicate-ID updates fail without collapsing either record. | Present envelopes/scalars, unsupported record versions, corrupt nested fields, unknown providers, or ambiguous mutation targets reject without dropping items or overwriting raw state. | Decode/storage/runtime failures reject visibly; no silent item filtering remains. | Uses the same lock and recovery-journal operation as `LS-006`, so no successful or failed import/delete can strand only one of Skills/Sources. | `core/skill/codec.ts`; `core/skill/sync-policy.ts`; `core/skill/registry.ts`; `core/sync/local-skill-merge.ts`; compatibility, sync-parity, duplicate-record, and cross-key fault/retry tests |
| `LS-008` `deepseek_pp_presets` | Released bare `SystemPromptPreset[]`; empty imported name/content strings, duplicate IDs accepted by earlier sync validation, and additive fields remain readable. | The sole Preset codec is shared by local storage, sync, Content, and Side Panel; successful unambiguous edits preserve additive fields and keep the released array shape, while duplicate-ID edits fail without collapsing records. | Present envelopes/scalars, unsupported per-record versions, corrupt fields, malformed IDs, or ambiguous edit targets reject before clocks or writes and leave raw state untouched. | Decode/storage/runtime failures reject; Preset UI retains its last confirmed snapshot rather than projecting empty success. Active-Preset responses decode the domain first, so a legal additive `ok:false` field cannot masquerade as a runtime failure. | Same-realm mutations share the local-state lock. Preset deletion and matching active-ID removal are one staged operation under the existing full-preimage recovery journal. | `core/preset/codec.ts`; `core/preset/store.ts`; shared runtime-response decoder; Background local-mutation runner; compatibility, repository, UI-boundary, duplicate-record, and cross-key fault/retry tests |
| `LS-009` `scenarioConfigs` | Released bare `ScenarioConfig[]`, merged with the three built-ins in their established order and Chinese labels. | The wire format remains the released bare array; saved built-in `enabled`/`template`, custom scenarios, and additive fields survive. Missing key returns defaults without a write. Side Panel mutations now cross the existing `SCENARIOS_UPDATED` command as an optional decoded request; the payload-less notification remains legal. | Present null/scalar/object envelopes, duplicate IDs, invalid field types, attempted future envelopes, or malformed mutation requests reject without fallback or overwrite. | Storage/codec failures reject; Scenario UI reports read/mutation failures and Background logs context-menu refresh/read failures. | Background is the sole cross-realm mutation authority and reuses the existing local-state lock/repository. Notifications occur only after commit; no second journal, key, or writer exists. | `core/scenario/codec.ts`; `core/scenario/runtime-request-codec.ts`; Background handler; compatibility, repository, Side Panel, and runtime-handler tests. |
| `LS-010` `deepseek_pp_tool_history` | Released bare history array, including older untrimmed or up-to-200-record legal input. | The authoritative tool-record validator decodes every row and preserves additive fields. Appends, reads, and clear share one Tool-History-local queue; legacy input is ordered by `createdAt` before retention, and new writes retain at most 100 records and 75% of the local-storage quota budget. | A present envelope/scalar, corrupt row, or duplicate ID rejects without removal or overwrite. No envelope is invented for the released bare array; a future schema needs an explicit migration. | Decode/storage failures reject; MCP UI shows the error and keeps its last confirmed history. If a provider already returned, a non-quota history failure is reported as non-retryable with an ambiguous external outcome. Quota-only oldest-record eviction remains explicit bounded best effort. | Whole-key rewrite has no rollback and evicted oldest records cannot be restored, but queued operations prevent same-realm lost appends and clear/write resurrection. Rejections do not poison later operations. | `core/tool/history-codec.ts`; `core/tool/history.ts`; raw compatibility fixtures; storage-authority, UI-error, restart, concurrency, retention, and budget tests |
| `LS-011` `deepseek_pp_usage_turns_v1` | Released bare v1 usage array; early missing optional/derived fields use the established read defaults. | One strict decoder preserves complete legal rows and additive fields while projecting only the released missing-field aliases. Records, reads, and clear share one Usage-local queue; writes prune to 5,000 records / 180 days, and quality-aware merging prevents a stale estimate from replacing newer server or metadata state. | A present envelope/scalar, corrupt row, invalid enum, invalid `YYYY-MM-DD` calendar day, or duplicate ID rejects without removal or overwrite. The bare-array schema is not silently reused for a future format. | Decode/storage/runtime failures reject. Content logs persistence failure and the write coordinator releases only its still-owned signature for retry. Usage UI keeps last-known data only for the same selected range; request generations fence slow range responses and all pre-clear reads. | Whole-key rewrite has no backup, but queued operations prevent same-realm lost records and clear/write resurrection. Rejections do not poison later operations, restart reads do not eagerly rewrite, and clear reloads the latest selected range. | `core/usage/codec.ts`; `core/usage/store.ts`; raw compatibility fixtures; storage-authority, stale-writer, UI-generation, error, and restart tests |
| `LS-012` `deepseek_pp_history_organizer` | Released versionless or `{schemaVersion:1,tagsBySessionId}` object; tag trimming, comma expansion, deduplication, and 12-tag bound remain unchanged. | One strict codec projects versionless input to v1 while preserving additive root fields and prototype-named session IDs as ordinary data keys. Missing key creates an in-memory empty state without eager write. | Future versions, null/scalar roots, corrupt tag maps, and non-string tags reject without defaulting or overwrite. | Load/save/decode failures remain latched in the visible status. The controller publishes new in-memory state only after storage confirms the write. | One content-realm FIFO re-reads and decodes confirmed storage inside each mutation, preventing same-realm lost writes and failed-write false success. R4.7 places that sole content-realm writer under the Content lifecycle; no Background/Side Panel writer or second command was added. | `core/history-organizer/codec.ts`; `entrypoints/content/adapters/history-organizer.ts`; controller ownership, raw compatibility, prototype-key, future/corrupt, write-failure, and queued-mutation tests |
| `LS-013` `dpp_tool_execution_blocks` | Released versionless persisted tool UI block array; legal rows carry the existing restore record plus `source:'storage'`, URL, and timestamp. | One strict codec/repository reads the whole array. Upsert validates first, serializes under the shared local-state lock, deduplicates by ID, and retains the newest 100 rows younger than 30 days without changing the storage shape. | A non-array root, malformed restore record, wrong source, invalid URL/timestamp field, or future incompatible row rejects without filtering, defaulting, or overwriting the raw key. | Read/write/decode failures are logged by the owning Content persistence boundary and remain rejected; fire-and-observe callers acknowledge only after that report. | Whole-key writes have no rollback; the confirmed raw key remains untouched on decode failure. A later legal upsert can retry after the underlying state is repaired. | `core/tool/execution-block-codec.ts`; `core/tool/execution-block-store.ts`; Content persistence and codec tests |
| `LS-014` `dpp_inline_agent_traces` | Released versionless inline-agent trace array, including active historical traces that restore as stopping/error. | One strict codec/repository validates trace, step, tool execution, status, correlation, timestamps, and counters. Upsert serializes under the shared local-state lock and retains the newest 100 traces younger than 30 days without changing prompt/continuation output. | A non-array root, malformed/future trace, unsupported status, or corrupt nested tool execution rejects the whole read before mutation; raw state is not filtered or rewritten. | Read/write/decode failures are logged by the owning Content persistence boundary and remain rejected. | Whole-key writes have no rollback; the confirmed raw key remains untouched on decode failure. Existing legal traces remain the recovery source. | `core/inline-agent/trace-codec.ts`; `core/inline-agent/trace-store.ts`; Content persistence, lifecycle, continuation, and prompt-freeze tests |

## Remaining Fixed `chrome.storage.local` Keys

| ID / key | Historical input and current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|
| `LS-015` `deepseek_pp_background` | Released partial background config normalizes to `{enabled,type,url,imageData,opacity}`; missing means null. | Unknown fields drop; values default/clamp. Preserve the key and current fields until versioned migration exists. | Storage errors reject. | Whole-key rewrite/remove; no backup. | `core/background/store.ts`; `core/background/config.ts` |
| `LS-016` `deepseek_pp_browser_control_settings` | Released settings normalize enabled, tab target, snapshot flag/node/text limits with safe defaults/clamps. | Unknown fields drop; future shape needs explicit codec/version before write. | Storage errors reject; invalid fields default silently. | Whole-key rewrite, no backup. | `core/browser-control/settings.ts`; browser-control tests |
| `LS-017` `deepseek_pp_chat_enabled` | Strict `true` enables; anything else disables. Disabling also removes cached headers. | Preserve strict boolean meaning. | Header cleanup failure is swallowed. | Main flag write succeeds independently of cleanup; no rollback. | `core/chat/store.ts` |
| `LS-018` `deepseek_pp_floating_chat_enabled` | Strict `false` disables; missing/other values enable for upgrade compatibility. | Preserve missing-as-enabled unless explicitly migrated. | Storage errors reject. | Whole-key rewrite, no backup. | `core/floating-chat/store.ts` |
| `LS-019` `deepseek_pp_model_type` | Only `expert|vision`; invalid/missing reads null. | New model values require explicit support; do not reinterpret old values. | Invalid values silently become null. | Set/remove one key. | `core/model/store.ts` |
| `LS-020` `deepseek_theme` | Only `light|dark`; invalid/missing reads null. | New values require explicit support. | Invalid values silently become null. | Whole-key rewrite. | `core/theme/store.ts` |
| `LS-021` `deepseek_pp_locale_preference` | `auto|zh-CN|en`; `auto` is represented by deleting the key. | Unknown locale becomes auto today; future locale support must not rewrite unknown data before user choice. | Invalid value silently defaults. | Remove/set one key. | `core/i18n/store.ts` |
| `LS-022` `deepseek_pp_prompt_injection_settings` | Released partial settings fill defaults for memory/system prompt, preset cadence, forced language. | Unknown fields drop; existing defaults/order remain stable. | Invalid fields default silently; storage errors reject. | Whole-key rewrite. | `core/prompt/settings.ts`; interactive tool tests |
| `LS-023` `deepseek_pp_voice_settings` | Released partial settings normalize input/read-aloud/rate/pitch and clamp numeric values. | Unknown fields drop; future voice settings need a codec/version. | Invalid fields default silently. | Whole-key rewrite. | `core/voice/settings.ts`; interactive tool tests |
| `LS-024` `deepseek_pp_pet` | Released partial `PetConfig`; size, opacity, position normalize/clamp. | Unknown fields drop; preserve missing-field defaults. | Storage errors reject. | Whole-key rewrite/remove. | `core/pet/store.ts`; `core/pet/config.ts` |
| `LS-025` `deepseek_pp_web_tool_settings` | Released `{web_search,web_fetch}` shallow-merges with defaults. | Persisted values are not currently type-checked; future fields must not widen tool enablement implicitly. | Bad values can reach runtime silently. | Whole-key rewrite. | `core/tool/web-settings.ts` |
| `LS-026` `deepseek_pp_official_api_chat_config` | Released model/thinking/reasoning-effort config; unknown enums default. | New enums require explicit support and should not erase unknown stored input. | Defaulting is silent. | Whole-key rewrite. | `core/chat/official-api-config.ts` |
| `LS-027` `deepseek_pp_official_api_key` | Trimmed string; empty/invalid means null. | Keep key identity; values are sensitive and require explicit migration if storage changes. | Storage errors reject; invalid reads become null. | Set/remove one key; no secret backup. | `core/chat/api-key.ts` |
| `LS-028` `deepseek_pp_multimodal_settings` | Released provider keys/models/base URLs normalize; writes reject invalid URL. | Unknown read fields default; provider/schema additions require explicit validation. | Invalid persisted data silently defaults; invalid write fails visibly. | Whole-key rewrite; no secret backup. | `core/multimodal/settings.ts`; multimodal settings tests |
| `LS-029` `deepseekCachedClientHeaders` | DeepSeek authorization/client header map captured from page traffic. | Keep exact key while used; unknown headers can remain strings. | Load/save/remove errors are swallowed. | Page reload/traffic can recapture; no durable rollback. | `core/deepseek/active-client.ts`; `core/deepseek/adapter.ts` facade; `core/chat/store.ts` |
| `LS-030` `deepseek_pp_active_preset_id` | Released absent key or raw string, including historical empty/dangling strings. | Reads preserve the string; user projection of empty/dangling state remains no active Preset. New non-empty writes must reference a decoded Preset; explicit clear remains allowed. | Present non-string, corrupt, or attempted future envelopes reject without removal or overwrite. | Decode/storage/runtime failures reject visibly in Content and Preset UI. | Matching Preset deletion and active-key removal share one journaled operation. Sync decodes the current raw key before mutation and clears a valid released string only when its target is absent from the downloaded Presets. | `core/preset/codec.ts`; `core/preset/store.ts`; sync local-state, repository, and cross-key recovery tests |
| `LS-031` `deepseek_pp_bundled_skill_enabled` | Map of Skill ID to boolean; non-boolean entries filter. | Unknown Skill IDs may remain; new value types require migration. | Filtering is silent. | Whole-key rewrite. | `core/skill/registry.ts` |
| `LS-032` `deepseek_pp_sync_config` | Released provider-less WebDAV and unversioned WebDAV/GDrive/OneDrive records read as an in-memory schema-v1 revision-0 projection without eager writes. The first explicit mutation preserves additive fields and writes the same top-level provider shape plus `schemaVersion:1` and a monotonic `revision`; `lastSyncAt` remains top-level. | Unknown schema/provider, partial provider fields, invalid timestamps/revisions, and non-JSON values fail before rewrite. Only schema v1 is writable. | Runtime targets are decoded and deep-cloned before queueing. Stale revisions fail visibly; lost write responses are accepted only after exact read-back proves the intended single-key value. | One Background FIFO owns config reads/writes and all test/auth/upload/download actions. Each mutation compares the expected revision and writes one key; no credential backup or second revision key exists. | `core/sync/config.ts`; `core/sync/operation-coordinator.ts`; config/coordinator/Settings tests |
| `LS-033` `deepseek_pp_whats_new_dismissed_version` | Version string; only current version suppresses the panel. | Unknown/old strings simply show the panel. | No error surface beyond panel state. | Set one key. | `core/whats-new.ts` |
| `LS-034` `deepseek_pp_whats_new_pending_version` | Version string; only current version means pending update. | Unknown/old strings are ignored. | No error surface beyond panel state. | Set/remove one key. | `core/whats-new.ts` |
| `LS-035` `pendingChatText` | Released single-slot string mailbox. Background commits the validated string before opening/notifying Side Panel; Side Panel initial read and storage subscription share one typed store/controller and clear only the generation it consumed. `OPEN_CHAT_WITH_TEXT` remains a compatibility hint, not a second truth. | Present non-string input rejects without cast or overwrite. Do not turn this released single slot into a durable queue without a versioned protocol. | Storage read/write/clear failures reject and surface through the owning Background/Side Panel path; expected missing notification receivers remain bounded best effort. | Storage is authoritative across lazy Side Panel startup. Generation fencing prevents a slow initial read from overwriting a newer storage event; the released single slot still intentionally provides no multi-message queue guarantee. | `core/chat/pending-text.ts`; Background delivery; `entrypoints/sidepanel/controllers/pending-text-controller.ts`; pending-text tests |

## Fixed `chrome.storage.session` Keys

| ID / key | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `SS-001` `deepseek_pp_active_chat_loop` | `{active:true,startedAt,provider?}`; missing provider means `web`. | Writes provider `web|official-api`; records older than 15 seconds are deleted and reported as interrupted. | Invalid values return no active loop. New providers need explicit normalization. | Storage errors reject; stale recovery is returned to caller. | Session-scoped only; clear removes marker, browser/session loss naturally resets it. | `core/chat/active-loop.ts`; `tests/chat-active-loop.test.ts` |
| `SS-002` `deepseek_pp_tool_authorizations` | Versioned, session-scoped background grants binding content-owned request identity, receiver-owned document/session, compact descriptor security digests, expiry, and payload-bound call reservation state. | Writes version 1; grants expire after 30 minutes, are capped at 32 active grants and 128 calls per grant, and the serialized state has a 4 MiB ceiling. Request terminal paths and agent completion explicitly close grants. The first external-payload chunk persists its collection binding; later exact receiver-bound chunks reuse an expiry-bounded in-memory proof, while a service-worker restart safely returns to the persisted check. | Missing state starts empty. Malformed, nested-corrupt, over-capacity, oversized, or unknown state fails visibly instead of being normalized or rewritten. A new-chat receiver can bind once to its assigned session; owner-document close still works after SPA route change. | Reservation write errors reject before provider execution. A completion write error retains the already-persisted `executing` replay barrier, logs explicitly, and does not replace the real provider result or skip history. | Browser-session loss clears all grants. Closed/expired grants and cached collection proofs fail closed; only an identical `web_fetch` permission precondition can retry once, after which the call is consumed. | `core/tool/authorization.ts`; `tests/tool-authorization.test.ts` |

`userToken` is DeepSeek page `localStorage`, not extension storage. `deepseek_pp_automation_wake` is an alarm name and `deepseek_pp_sync` is a multipart boundary; neither is counted as a storage key.

## Sync Snapshot Contracts

| ID / status | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `SY-001` `memories.json` required | Array of memories; historical numeric `id` is accepted then removed; missing scope remains the released `global` alias; `syncId` is required and an empty description is legal. | The sole Memory codec returns `Omit<Memory,'id'>[]`. Local staging reuses numeric IDs by ordered `syncId` occurrence, preserves local additive fields unless remote data replaces them, and deterministically allocates IDs for additional duplicate occurrences. | Any invalid remote item or corrupt/future local Memory state rejects before journal preparation or replacement. No file envelope version is added. | Download fails before local mutation when parsing, current-state validation, or ID staging fails. | Raw rows and IDs are journaled before replacement; every pre-commit failure and worker restart restores them exactly. | Memory codec; background sync flow; local-state, schema, repository, and IndexedDB compatibility tests. |
| `SY-002` `skills.json` required | Released bare syncable custom/remote Skill array; local-provider remote Skills remain local. | Uses the same exact-preserving collection codec as `LS-006`, including deep remote metadata; local imports merge before journal preparation. | Any corrupt/future item or non-array root rejects the whole file. No sync envelope or parallel validator exists. | Parse/apply errors are visible and Content/Side Panel keep last confirmed state. | Raw Skill state is restored by the existing journal after any local-apply failure; ordinary paired mutations use that same journal. | Skill codec; Background sync flow; raw compatibility, local-Skill merge, parity, and local-apply tests |
| `SY-003` `skill-sources.json` optional | Released bare non-local source array; absent file means an empty remote set while local sources remain preserved by merge. | Uses the same provider-specific codec as `LS-007`; local sources are decoded then excluded from cloud upload/download truth. | Any corrupt/future item or non-array root rejects the whole file without item filtering. | Parse/apply errors are visible. | Raw source state is journaled and restored with Skills in deterministic reverse order; ordinary source mutations reuse the same protocol. | Source codec; Background sync flow; sync parity, local-Skill merge, and cross-key recovery tests |
| `SY-004` `presets.json` required | Released bare Preset array plus the separate local active-Preset selection. | Uses the same exact-preserving codec as `LS-008`; a missing target clears a valid released string active ID in its own staged step. | Invalid/future Presets or corrupt/future local active-ID state reject before target writes; opaque state is never reinterpreted or cleared. | Parse/apply errors are visible and receiving surfaces retain last confirmed state. | Presets and active-ID retain separate raw preimages/fault boundaries inside the existing one-operation journal. | Preset codec/store; Background sync flow; local-state, compatibility, and fault/retry tests |
| `SY-005` `project-context.json` optional | Missing file preserves local without rewriting it; released v1 and versionless/v2 are accepted by the same Project codec as local storage. | Valid input becomes the lossless v2 projection described by `LS-004`. | Future/corrupt input rejects before journal preparation. During apply, an existing local future/corrupt raw value also blocks replacement. | Sync parse/apply failures are visible and fail-closed. | When present, its opaque pre-download key is journaled; when absent, no target write occurs. Any later apply failure restores the original opaque value. | Project domain codec; local-state, recovery, compatibility, and repository tests. |
| `SY-006` `saved-items.json` optional | Missing file preserves local without rewriting it; bare array, versionless object, and v1 object share the same codec as local storage. | Valid input becomes v1 without normalizing existing values. | Explicit non-v1 and corrupt items reject before journal preparation; an existing local future/corrupt raw value blocks replacement. | Sync parse/apply failures are visible. | When present, its opaque pre-download key is journaled; when absent, no target write occurs. Any later apply failure restores the original opaque value. | Saved Items domain codec; local-state, recovery, compatibility, and repository tests. |
| `SY-007` Preserve — generation and local apply | Legacy input is three required and three optional fixed files. Current uploads write six generation-scoped payloads, one checksum manifest, then `sync-current.json` as the remote commit point. | Readers validate the complete remote generation before local mutation. Local apply and Project/Memory/Skill/Source/Preset cascades share one prepared full-preimage journal, reverse recovery, and journal deletion as the local commit point. Startup recovery gates runtime dispatch, stale-Memory archival, automation scans, and cascade responses. | Unknown/corrupt remote envelopes and unknown/corrupt local journals fail closed without fallback or deletion. Generation and local journal schema v1 are the only supported envelopes. | Every remote and local boundary rejects visibly. Lost prepare/clear responses are verified; an unverifiable clear reports commit-indeterminate and the recovery barrier rechecks it. Every successful recovery check, including a confirmed absent journal, waits for one state-refresh attempt so a committed clear cannot leave live consumers stale after worker restart. | Upload leaves the prior pointer authoritative after every pre-commit failure. Download and generic mutation restore raw Memory/key preimages after injected failures or restart; a cleared journal keeps the fully applied target. | generation publisher/reader; local apply coordinator/adapter/runtime; background recovery barrier; exhaustive sync and generic-mutation fault tests. |
| `SY-008` Preserve — confirmed target and action order | Test/auth capture the target on click; upload/download capture it before the destructive confirmation opens. Every request carries that immutable config plus the last observed config revision; upload/download are no longer payload-less commands and Settings no longer sends save-then-action pairs. | The receiving codec rejects missing/malformed targets before privileged work. One FIFO serializes config mutation and complete remote/local actions, so an older snapshot cannot publish after a newer accepted action. OAuth access-token cache entries also match a SHA-256 refresh-token identity, never the raw token. | Form changes after confirmation, cross-window revision conflicts, provider faults, apply faults, and queue recovery failures remain visible. A known config baseline committed before a later failure returns its revision and `lastSyncAt`. If OAuth/sync effect completion is followed by a conflicted or indeterminate config write, the response instead marks `effectCompleted` plus `reloadConfig`, and Settings rereads the FIFO-owned authority without inventing a revision. | Existing generation pointer, legacy-pointer-absence read, local journal, raw rollback, and recovery barrier are reused unchanged. Upload snapshots are captured under the existing local-state lock; no second publisher, journal, router, or validator is introduced. | sync config/coordinator, Background composition, Side Panel target tests, OAuth identity test, generation/local-apply regression suites. |

## Import and Export Contracts

| ID / status | Historical input | Current output | Unknown/future behavior | Failure visibility | Recovery/rollback | Evidence |
|:--|:--|:--|:--|:--|:--|:--|
| `EX-001` Preserve — Memory JSON | Released bare arrays may contain exported `Memory` rows with numeric IDs/timestamps/access stats or Settings preview-shaped `NewMemory` rows. | One Memory import codec accepts both legal forms, preserves `syncId` when present, discards local numeric IDs, and rebuilds local timestamps/access stats for the appended rows. The complete batch is validated before one locked Dexie transaction. | No new envelope/version is invented. Non-array, corrupt/future-shaped records, or unsupported scope/project combinations reject without writing. Duplicate sync IDs retain released append semantics. | Parse, codec, current-table, or transaction failure is reported to Settings; the success count changes only after commit. | Every row in the batch commits or none does. Concurrent imports serialize without interleaving, and the broadcast happens only after the transaction commits. | `core/memory/codec.ts`; `core/memory/store.ts`; Settings/background import flow; Memory repository fault/concurrency tests. |
| `EX-002` Preserve — Conversation model | Schema literal `deepseek-pp.conversation-export.v1`. | Internal validated model renders Markdown, HTML, PDF, or image manifest. | Unknown schema rejects; nested validation is currently partial and needs fixtures before widening. | Session/attachment collection can record localized failures; formatting cancellation fails closed. | Export is read-only; retry recreates artifact. | `core/export/types.ts`; `core/export/schema.ts`; conversation export tests |
| `EX-003` Current-only — Saved Items JSON | No importer exists. | Exports `{schemaVersion:'deepseek-pp.saved-items-export.v1',exportedAt,items}`. | Version is only an output marker until a validator/importer exists. | Generation errors reject. | Read-only; regenerate. | `core/export/secondary-artifacts.ts`; product-surface tests |
| `EX-004` Current-only — secondary text | Message Markdown, Saved Items Markdown, image-manifest HTML. | Human-readable artifacts. | Not a round-trip persistence contract; no importer should infer one. | Generation errors reject. | Read-only; regenerate. | `core/export/secondary-artifacts.ts` |

## Sensitive-Data Note

`LS-003`, `LS-027`, `LS-028`, `LS-029`, and `LS-032` currently persist MCP secrets, API keys, authorization/client headers, or sync credentials in extension local storage. This registry records the compatibility surface; it does not claim at-rest secrecy. Any security migration must preserve recoverable user configuration, avoid logging secret values, and make destructive credential reset explicit.

## Current Evidence Limit

T1.4 adds raw historical fixtures for Memory v1-v3, Artifact legacy storage, Project v1/v2, Saved Items legacy/v1/future, Scenario storage, all six sync JSON files, and the released sync-config shapes. Fake IndexedDB tests execute the production Memory v1→v3 and v2→v3 upgrades, v3 project-scope reopen, and Artifact legacy migration. T2.4 adds an independent raw schema-v1 generation fixture, exhaustive upload boundaries, strict generation reads, legacy no-dual-write compatibility, and newest-live Google Drive canonical-object selection for duplicate names. T2.5 adds the raw schema-v1 local journal fixture, exact Memory/key preimage recovery, deterministic ID reuse, every-write fault injection, restart recovery, and idempotent retry evidence. R3.5 closes the Project v1 reset, Saved Items future downgrade, and Scenario broad fallback with shared codecs, exact-preserving fixtures, no-eager-write reads, fail-closed replacement guards, same-realm serialization, and visible UI/background errors. R3.6 closes Memory batch partial writes, duplicate validators, Artifact filtering/no-retry/dual truth, and the Project/Memory partial cascade with exact codecs, explicit native DB-version guards, one IndexedDB authority, and the existing full-preimage journal. R3.7 adds the single-key sync-config migration/CAS authority, immutable runtime action target, full-action FIFO, locked local snapshot capture, Side Panel fencing, and credential-bound OAuth token cache while retaining the T2.4/T2.5 commit protocols. R3.8 adds raw Automation/Usage/Tool-History fixtures, exact released alias projections, strict future/corrupt rejection, one reusable FIFO primitive with independent authorities, stale-writer fencing, visible receiver errors, restart/concurrency evidence, and range/clear generation fencing without changing released storage shapes. R3.10 adds exact released Skill/Source/Preset/active-ID/History fixtures, one codec and local-only policy per concept, duplicate-record preservation, strict receiving boundaries, confirmed-state History serialization, and paired-mutation fault/restart coverage without adding a storage envelope or journal.

Future Memory/Artifact database versions are explicitly rejected and preserved; adding support still requires a deterministic identity-preserving upgrade rather than a permissive open. R4.4 centralizes Scenario mutations in Background, R4.7 puts the sole History writer under Content lifecycle ownership, and R4.9/R4.11 fence Side Panel async/runtime state. R4.6 adds strict versionless tool-block and inline-trace codecs without changing their released keys or shapes. R6.5 coalesces only adjacent unobserved Usage/Tool-History mutations; reads, clears, failures, Sync, journals, and confirmed-target ordering remain barriers. Generation garbage collection, a replacement logical Memory ID allocator, and distributed conflict resolution remain out of scope. Orphaned remote objects and prepared local journals are never reported as committed success.
